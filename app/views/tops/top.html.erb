<div id="heropattern", class="min-h-screen pt-10">
  <div class="text-9xl mt-10">研究所へようこそ</div>
  <div class="flex flex-col my-20">
    <%= link_to "カロリーカウンターAIへ",calorie_counter_chats_path, class: "btn btn-ghost normal-case text-xl m-4", data: { turbo: false }   %>
    <%= link_to "ポモドーロタイマーへ",pomodoro_timer_timers_path, class: "btn btn-ghost normal-case text-xl m-4", data: { turbo: false }  %>
    <%= link_to "最強に面白いアプリへ",saikyouni_omoroi_app_path, class: "btn btn-ghost normal-case text-xl m-4", data: { turbo: false }  %>
  </div>
</div>


<script>
let body = document.getElementById('theme-body');
let toggle = document.getElementById('themeToggle');
let themes = ["dark","synthwave","cyberpunk","valentine","halloween","forest","aqua","black","dracula","night"];
let heropattern = document.getElementById('heropattern');

window.addEventListener('DOMContentLoaded', function() {
  if (document.cookie.indexOf('selectedTheme=light') >= 0) {
    toggle.checked = true;
    heropattern.classList.remove('heropattern-floatingcogs-gray-600');
    heropattern.classList.add('heropattern-floatingcogs-gray-200');
  } else {
    toggle.checked = false;
    heropattern.classList.remove('heropattern-floatingcogs-gray-200');
    heropattern.classList.add('heropattern-floatingcogs-gray-600');

  }
});

toggle.addEventListener('change', function() {
  let date = new Date();
  date.setTime(date.getTime() + (365 * 24 * 60 * 60 * 1000));
  let expires = "; expires=" + date.toUTCString();

  // Select a random theme from the themes array.
  let theme = themes[Math.floor(Math.random() * themes.length)];

  if (this.checked) {
    body.setAttribute('data-theme', 'light');
    document.cookie = 'selectedTheme=light' + expires + '; path=/';
    heropattern.classList.remove('heropattern-floatingcogs-gray-600');
    heropattern.classList.add('heropattern-floatingcogs-gray-200');
  } else {
    body.setAttribute('data-theme', theme);
    document.cookie = 'selectedTheme=' + theme + expires + '; path=/';
    heropattern.classList.remove('heropattern-floatingcogs-gray-200');
    heropattern.classList.add('heropattern-floatingcogs-gray-600');
  }
});
</script>